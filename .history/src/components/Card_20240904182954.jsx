import { React } from "react";

const Card = () => {
  return (
    <div className="card">
      <Card title="卡片标题" onClick={()=>{}}>
        卡片内容
      </Card>
    </div>
  );
};

export default Card;
